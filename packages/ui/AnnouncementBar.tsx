// The announcement bar was retired with the 3-page marketing rebuild.
// Kept as a no-op so the layout import stays valid and it can be revived later.
export default function AnnouncementBar() {
  return null;
}
